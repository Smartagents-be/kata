import type { Step } from '@/shared/step'
import html from './content.html?raw'

/** Graded by be.smartagents.kata.java.step1.FizzBuzzChecker. */
const step1: Step = {
  id: 'step1',
  title: 'Warm-up: FizzBuzz',
  html,
  exerciseId: 'fizzbuzz',
}

export default step1
