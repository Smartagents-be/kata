package be.smartagents.kata.java.step2.domain;

/** What kind of thing was borrowed. Each kind is charged its own way once it is overdue. */
public enum MediaType {
    BOOK,
    DVD,
    REFERENCE,
    EQUIPMENT
}
