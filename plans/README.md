# Implementation Plans

Generated by the improve skill on 2026-08-02, against commit `be5a6b2`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

Two repo-wide rules every executor inherits, whatever the plan:

1. **Never quote, decode, move, or aggregate a flag** (a leetspeak `{…}`
   string). Several live in plaintext in files you may read; that is exercise
   design, not an accident.
2. **Never edit `audit.md`** in a plan's commit. Fixing and re-measuring are
   separate jobs here; the maintainer runs the `audit-update` skill.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Untrack local settings, unstage video renders | P1 | S | — (run in real checkout, not a worktree) | BLOCKED (stale: commit `22bf8a8` committed `video/out copy2/` + the video project to main, so unstaging no longer applies; the settings.local.json half is still valid — plan needs a reconcile rewrite) |
| 002 | shadcn to devDependencies + patch bumps | P1 | S | — | TODO |
| 003 | CI for the five documented builds | P1 | S | — | TODO |
| 004 | vitest + `prepareUnit` characterization | P1 | M | — | TODO |
| 005 | Unit-HTML content-contract checker | P1 | S | 004 | TODO |
| 006 | Flag-grading + reset contract tests | P2 | S | 004 | TODO |
| 007 | Re-true the intent docs | P2 | S | — | TODO |
| 008 | Root README | P2 | S | 007 (soft: links stay fresh) | TODO |
| 009 | Catalog fetch timeout | P2 | S | — (004 enables its test) | DONE (executed 2026-08-02, commit `9aa668a` on `advisor/009-catalog-fetch-timeout`, reviewed and approved; not merged) |
| 010 | Lazy-load `/present` | P2 | S | — | TODO |
| 011 | Resize unit screenshots | P3 | S | — | TODO |
| 012 | Storage writes out of state updaters | P3 | S | 006 (soft: its tests guard this) | TODO |
| 013 | Copilot step 2 parity scoping (spike, report only) | P2 | M | — | TODO |
| 014 | Deck assistant filter + step 2 deck scope | P3 | S+M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 first, before any commit on the branch**: 19 MB of media is staged
  right now; any commit before 001 bakes it into history.
- 005 and 006 need 004's runner (vitest/jsdom).
- 012 is safest after 006, whose FlagBoard persistence tests are its net.
- 003's CI gains steps as others land: `npm audit --omit=dev --audit-level=high`
  after 002, `npm test` after 004, `npm run check:content` after 005 (each
  noted in 003's maintenance section).
- 013 and 014 produce reports into `plans/reports/`; the authoring work they
  scope is deliberately NOT planned here — it is curriculum writing that needs
  the maintainer's voice and the `lesson-writing` / `quiz-writing` skills.

## Deferred findings (real, not planned this round)

- **Consolidate the two FlagBoards** into `shared/` (step 1's is a hard-coded
  twin of step 2's parameterized one). Real debt, MED risk on grading code;
  do only after 006's tests exist. Salts, storage keys and element ids must
  stay byte-identical.
- **Resolve the `shared` -> `@/steps` seam**: six `shared/` files import the
  steps barrel while three comments state "shared never imports a step".
  Either correct the comments (the narrow rule is about locales/content) or
  split a manifest module; the split is also what unblocks route-level code
  splitting of step content and locales (~167 KB Dutch JSON in every visitor's
  bundle). Needs a maintainer call on which the rule is.
- **Escape the i18n -> innerHTML path** (`escapeValue: false` +
  `element.innerHTML = translated` in `content.ts`): no live vulnerability
  (verified: no interpolation params reach prose keys), guarded by convention
  only. Cheap hardening if wanted.
- **XML parser DOCTYPE hardening** in step 2's `FlagRevealIT` (build-time
  reports only; defensive).
- **`CatalogRun` edge-case pins** (marker case/whitespace) — black-box tests
  only; the step 1 prohibitions forbid seams, so a seeded-random refactor is
  off the table.
- **Toolchain pins** (`.nvmrc`, `engines`; Maven enforcer would honor the
  no-wrapper decision but changes a surface `CLAUDE.md` documents as bare —
  maintainer call).
- **`.editorconfig` / formatter**: editorconfig is free; a formatter needs the
  vendored/generated exclusions and must stay away from unit HTML until 005's
  checker can catch a bad reflow.
- **Tracking decision for `.agents/`, `skills-lock.json`,
  `.claude/skills/improve`, and the whole `video/` project** (558 MB
  untracked): both "commit the sources" and "ignore wholesale" are fine;
  the undecided state is what costs. Maintainer decision; 001 handles only the
  staged media.
- **`LateFeeReport` investigation** (step 2): referenced by nothing, counted by
  the graded coverage floor, Javadoc calls it "the one use case" while the
  controller uses `MemberStatements`. May be deliberate exercise calibration —
  changing JaCoCo scope moves the capstone's difficulty, so this is a question
  for the maintainer, not a fix.
- **LICENSE**: absent; choosing one is the maintainer's legal call (008
  deliberately does not).
- **Direction items not selected this round**: instructor scaffolding
  (audit item 68), a step 2 quiz layer (item 36), the soft-skills content
  holes (items 42/43/39), and the video project's placement decision — all
  grounded in the maintainer's own audit; revisit any of them by name.

## Findings considered and rejected

- **`ExercisePanel`'s `key={detail}` duplicate-key bug**: real, but root
  `CLAUDE.md` records the component is parked pending a decision ("leave them
  alone in either direction"). Fix it the day the component is revived.
- **Softening the `welcome` unit's assistant promise**: prohibited by
  `front/src/steps/step0/CLAUDE.md` ("the repair is giving step 2 variants…
  do not restore a scope line as a substitute for the work"). Plan 013 scopes
  the recorded repair instead.
- **TypeScript 7 upgrade**: tilde pin is deliberate; a major rewrite with no
  test net underneath. Not now.
- **radix-ui umbrella / lucide-react bundling**: verified tree-shaken; no cost.
- **`prepareUnit` re-parsing / provider re-renders**: verified correctly
  memoized; no finding.
- **CSP / response headers**: static SPA, no auth, no cookies; nothing behind
  the header. Revisit only if the Java service leaves localhost.
- **Java performance**: nothing accidentally quadratic at nine titles;
  verified no cross-request mutable state in the step 1 pipeline.
- **Step 1 obfuscation, step 2 red profiles, duplicated Veil/Scramble, absent
  root pom, dark-mode tokens, unnamed step 0 profile**: all recorded design
  decisions, not findings; re-auditing them is wasted work.

## Audit scope note

Standard-depth audit (4 subagents: correctness+security, perf+tech-debt,
tests+deps+DX, docs+direction), findings vetted against the code by the
advisor. Not audited: `video/`'s own source and dependency tree (pending the
placement decision), the ~90 SVG figure components line-by-line (scanned for
effects/cleanup only), Dutch locale content, the graded profile's
JaCoCo/PIT thresholds (needs a Maven run), and curriculum prose quality
(owned by `audit.md`).
