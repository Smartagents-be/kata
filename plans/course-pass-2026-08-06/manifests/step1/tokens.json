{
  "unit": "step1/tokens",

  "deck": [
    {
      "file": "front/src/steps/step1/deck.tsx",
      "action": "insert",
      "where": "Immediately after the slide object with id 'deck-tokens-split' (currently ends at the line `scale: 1.77,` followed by `},` around line 105), and before the object with id 'deck-tokens-next'.",
      "code": "  {\n    id: 'deck-tokens-language',\n    kind: 'statement',\n    ns: 'step1',\n    eyebrow: 'tokens.title',\n    // The unit's one prose argument and the step's first actionable move. Without it a room meets\n    // `ask in the language it has read most of` for the first time in `recap`.\n    title: 'deck.tokens.language.title',\n    note: 'deck.tokens.language.note',\n  },",
      "keys": "deck.tokens.language.title and deck.tokens.language.note are supplied in patches/step1/tokens.json, in both bundles."
    },
    {
      "file": "front/src/steps/step1/deck.tsx",
      "action": "refit",
      "where": "the slide with id 'deck-tokens-split'",
      "reason": "`TokenSplit` grew a four-row rate strip between the chips and the panel, so it is taller than it was when 1.77 was fitted, and `SlideFigure` clips rather than shrinks.",
      "instruction": "Put the slide on the projector and check the caption still lands inside the frame. If it does not, drop `scale` from 1.77 until it does; about 1.4 is the expected landing point."
    }
  ],

  "claudeMd": [
    {
      "file": "front/src/steps/step1/CLAUDE.md",
      "action": "replace",
      "reason": "The eyebrow quoted there is no longer the label. `words-into-tokens.label` now reads `one word through the model`, because `tokenization process` named stages one and two of six and left the forward pass, which is the payoff, unnamed.",
      "old": "it read `text at both ends,\nnumbers in between` and now reads `tokenization process`, because a label that argues the picture is\none more thing to read before the picture.",
      "new": "it read `text at both ends,\nnumbers in between`, then `tokenization process`, and now reads `one word through the model`. The\nmiddle one named the first two stages and left the forward pass, which is the payoff, unnamed. A\nlabel that argues the picture is one more thing to read before the picture, so this one stays a\nname."
    },
    {
      "file": "front/src/steps/step1/CLAUDE.md",
      "action": "append-to-paragraph",
      "reason": "The rate strip is a new decision in `TokenSplit` and it sits right next to the standing prohibition on a Dutch row, which is exactly where a future editor will look.",
      "anchor": "the paragraph about `TokenSplit`, directly after the sentence ending \"and it would cost the figure the same way it did the first time.\"",
      "new": " **The rate strip under the chips is a rate readout rather than a second sample.** Four rows, tokens per hundred characters, all four always up on one scale with only the emphasis following the selection: the section's claim is comparative and a panel showing one sample at a time left the reader to click, remember and subtract. The numbers are worked out from the same `SAMPLES` the chips are drawn from, so the strip cannot drift from the panel above it, and `lead.3`'s band was widened to four to six characters per token to hold the 6.1 the prose row prints."
    },
    {
      "file": "front/src/steps/step1/CLAUDE.md",
      "action": "replace",
      "reason": "Both forward pointers are anchors now rather than mono unit names. JetBrains Mono means `what the machine produced`, and a unit name is not that.",
      "old": "each in one paragraph naming the unit that owns it.",
      "new": "each in one paragraph carrying an anchor to the unit that owns it. They were set in mono once, which said a machine had produced the name."
    }
  ],

  "notes": [
    "units/tokens.html, TokenSplit.tsx, NextToken.tsx and PickTheNext.tsx were edited directly by this agent; nothing in them is left for the integrator.",
    "`tokens.one-at-a-time.2` was cut and `.3`/`.4`/`.5` renumbered down one. `NextToken.tsx` and `PickTheNext.tsx` cited `tokens.one-at-a-time.4` in their doc comments and now cite `.3`; both are already fixed.",
    "No quiz change and no TaskCard: the work order forbids both for this unit."
  ]
}
